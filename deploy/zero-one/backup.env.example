# BACKUP_DIR must be a real off-host filesystem mount with .offsite-mounted.
# Only the public age recipient belongs on the production host.
BACKUP_DIR=/mnt/offsite/zero-one
BACKUP_AGE_RECIPIENT=REPLACE_WITH_OFFLINE_RECOVERY_PUBLIC_RECIPIENT
